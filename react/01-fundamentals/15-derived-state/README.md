# Deriving State

This app explores deriving a value (`isEven`) from existing state during
render, instead of storing it in its own `useState` and keeping it in sync
manually.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
