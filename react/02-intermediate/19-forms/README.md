# Forms

This app explores controlled inputs: the input's `value` comes from state
and `onChange` updates that state, keeping the two in sync. It also covers
handling form submission with `onSubmit` and `event.preventDefault()`.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
