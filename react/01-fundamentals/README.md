# Fundamentals

Quickstart React apps, each exploring one idea building on the previous ones.
Every app is a standalone Vite + React project - see its own README for details.

## Getting started

Each app is self-contained:

```
cd <app-folder>
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
