# Conditional Rendering

This app explores three ways to render content conditionally: the ternary
operator (`? :`), the `&&` short-circuit pattern, and an early `return null`
inside a component (`Alert`).

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
