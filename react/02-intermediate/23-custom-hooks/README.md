# Custom Hooks

This app explores extracting reusable stateful logic into a custom hook,
`useCounter`. It's just a function starting with `use` that calls other
hooks internally - each call in `App` gets its own independent state.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
