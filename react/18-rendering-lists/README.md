# Rendering Lists

This app explores turning an array of data into JSX with `.map()`, and why
each resulting element needs a stable, unique `key` (here, `fruit.id` rather
than the array index).

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
