# Reducer

This app explores `useReducer`: state updates are centralized in a single
`reducer` function, and triggered by dispatching action objects instead of
calling several separate setter functions.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
