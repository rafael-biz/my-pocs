# Refs

This app explores `useRef` two ways: attaching it to a DOM node to imperatively
call `.focus()`, and using it to store a mutable value (`renderCount`) that
persists across renders without causing a re-render when it changes.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
