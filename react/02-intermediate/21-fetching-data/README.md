# Fetching Data

This app explores the loading/error/data pattern for fetching data in an
effect that runs once on mount, updating state as the request settles.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
