# Destructuring Props

This app explores destructuring props directly in the function parameter
list, and spreading an object as a component's props.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
