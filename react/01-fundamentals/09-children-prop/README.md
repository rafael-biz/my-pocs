# Children Prop

This app explores component composition using the special `children` prop:
`Card` wraps whatever JSX is placed between its opening and closing tags
without needing to know what that content is.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
