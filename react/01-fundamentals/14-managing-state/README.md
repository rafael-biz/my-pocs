# Managing State

This app explores `useState`: it returns the current value and a setter
function, and calling the setter tells React to re-render with the new
value - fixing the problem shown in `13-not-updating-ui`.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
