# Dynamic Styles

This app explores styling based on state, two ways: toggling a CSS class
via a dynamic `className`, and setting a value directly via an inline
`style` object.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
