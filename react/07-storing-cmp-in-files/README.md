# Storing Components in Files

This app explores splitting a component out into its own file under
`src/components/` and importing it where it's used.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
