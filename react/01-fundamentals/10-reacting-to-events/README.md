# Reacting to Events

This app explores handling DOM events in React by passing a function
reference (or an inline arrow function) to a prop like `onClick`.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
