# Routing

This app explores basic client-side routing with `react-router-dom`:
`BrowserRouter` enables navigation, `Link` swaps the URL without a full page
reload, and `Routes`/`Route` render the matching page component.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
