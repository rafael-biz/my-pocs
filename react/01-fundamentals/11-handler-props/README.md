# Handler Props

This app explores passing functions down as props: `App` owns the event
handlers, and `Button` just calls whichever one it receives via `onClick`.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
