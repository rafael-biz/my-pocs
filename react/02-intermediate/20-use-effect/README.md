# useEffect

This app explores `useEffect` for side effects: one effect syncs the
document title whenever `count` changes (dependency array), and another
starts an interval once on mount and clears it via the effect's cleanup
function.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
