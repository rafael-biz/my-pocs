# Dynamic Attributes

This app explores setting HTML attributes dynamically, including importing
and using image files as the `src` of an `<img>`.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
