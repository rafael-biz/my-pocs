# Styles Next to Component

This app explores co-locating a CSS file next to the component that uses it
(`Greeting.jsx` + `Greeting.css`) and importing it directly in the component.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
