# Component Props

This app explores passing data into a component via props and reading it
through the component's `props` parameter.

## Getting started

```
bun install
bun run dev
```

Then open http://localhost:5173 or Start Debugging with VSCode.
